import { ORG_EMAIL, ORG_LOGO, ORG_NAME } from "../lib/constants"

export default function Error(params) {
    return <h3>" ERROR 404 - We will be Back Shortly "<br />
                Feel  free to Contact Us at  {ORG_EMAIL}
    </h3>;

}